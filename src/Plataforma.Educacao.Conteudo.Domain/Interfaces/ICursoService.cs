namespace Plataforma.Educacao.Conteudo.Domain.Interfaces
{
    public interface ICursoService : IDisposable
    {
        //Task<bool> AdicionarCurso(string nome, decimal valor, DateTime? validoAte, Guid conteudoProgramaticoId);
        //Task<bool> AlterarCurso(Guid cursoId, string nome, decimal valor, DateTime? validoAte);
        //Task<bool> AtivarCurso(Guid cursoId);
        //Task<bool> DesativarCurso(Guid cursoId);
        //Task<bool> RemoverCurso(Guid cursoId);
        //Task<bool> AdicionarAula(Guid cursoId, string descricao, short cargaHoraria, byte ordemAula);
        //Task<bool> AlterarAula(Guid aulaId, Guid cursoId, string descricao, short cargaHoraria);
        //Task<bool> AtivarAula(Guid aulaId);
        //Task<bool> DeativarAula(Guid aulaId);
        //Task<bool> RemoverAula(Guid aulaId);
    }
}
